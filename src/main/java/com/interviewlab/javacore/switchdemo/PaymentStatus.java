package com.interviewlab.javacore.switchdemo;

/** Modern (Java 21) switch expression exhaustiveness'ini göstermek için küçük bir enum. */
public enum PaymentStatus {
    PENDING, COMPLETED, FAILED, REFUNDED
}
